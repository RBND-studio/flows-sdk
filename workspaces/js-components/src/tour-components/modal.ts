// TODO: add exit nodes
export interface ModalProps {
  title: string;
  body: string;
  continueText?: string;
  previousText?: string;
  showCloseButton: boolean;
  hideOverlay: boolean;
}

// export const Modal = () => {};
